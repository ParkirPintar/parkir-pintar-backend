apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: parkir-pintar
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9091"
    spec:
      containers:
        - name: payment-service
          image: parkirpintar/payment-service:latest
          ports:
            - containerPort: 50051
              name: grpc
          envFrom:
            - configMapRef:
                name: parkirpintar-config
            - secretRef:
                name: payment-db-secret
            - secretRef:
                name: redis-secret
          livenessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: parkir-pintar
spec:
  selector:
    app: payment-service
  ports:
    - port: 50051
      targetPort: 50051
      name: grpc
  type: ClusterIP
