apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: grafana
  namespace: monitoring
spec:
  hosts:
    - "*"
  gateways:
    - parkir-pintar/parkirpintar-gateway
  http:
    - match:
        - uri:
            prefix: /monitor
      route:
        - destination:
            host: grafana.monitoring.svc.cluster.local
            port:
              number: 3000
